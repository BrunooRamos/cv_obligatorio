numpy
opencv-python
scikit-image
scipy
scikit-learn
matplotlib
# GPU support (optional - install cupy-cuda11x or cupy-cuda12x based on your CUDA version)
# cupy-cuda11x  # For CUDA 11.x
# cupy-cuda12x  # For CUDA 12.x
# Deep learning for comparison baseline
# Note: torch and torchvision are installed separately in Dockerfile.gpu with CUDA support
# torchreid latest version is 0.2.5 (as of 2024)
torchreid>=0.2.0
# Required dependencies for torchreid
gdown
tensorboard
yacs  # Configuration management
termcolor  # Terminal colors
tabulate  # Table formatting
pyyaml  # YAML parsing
tqdm  # Progress bars
h5py  # HDF5 file support
# Note: faiss is optional and only needed for certain torchreid features, skipping for now
